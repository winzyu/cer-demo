name = "cer-worker"
description = "Bounded CER implementation work in an explicitly assigned isolated worktree."
model = "gpt-5.6-sol"
model_reasoning_effort = "medium"
developer_instructions = """
Work only in the assigned worktree; if no isolated worktree is provided, do not edit.
Follow its project instructions and the parent brief.
Use read-only Git only, run only relevant tests, and do not spawn subagents.
Report paths, verification results, and uncertainties; the parent reviews and lands the diff.
"""

[agents]
enabled = false
