# Status

Current state and next steps only.
Rewritten at the end of every session by `/handoff`; history is `git log -p docs/STATUS.md`.
Never cite this file from code or other docs: the reasoning lives in the docs under "Where things live".

Updated 2026-09-24 (late) at `dev` commit `dd4b51a` plus this handoff, by the release orchestrator session.

## Start here

- **Gilligan release, September 30.** The task list, owners and dates are [`migration/GILLIGAN_RELEASE_PLAN.md`](migration/GILLIGAN_RELEASE_PLAN.md) (task IDs such as Q1, S3, L5); where this file and the plan disagree, the plan wins. The user deploys.
  Background: [`migration/GILLIGAN_PRODUCT_DIRECTION.md`](migration/GILLIGAN_PRODUCT_DIRECTION.md), then [`migration/GILLIGAN_TARGET_ARCHITECTURE.md`](migration/GILLIGAN_TARGET_ARCHITECTURE.md).
- **Orchestration.** One session reviews each workstream's actual diff when it reports done, lands cer-demo branches on `dev` one at a time in dependency order (Q1 before Q3-Q6; prompt changes before R4's final capture), runs typecheck, lint and the touched suites singly after each merge, pushes `dev`, and keeps the plan current. It never pushes upstream and implements nothing itself beyond small fixes.
  Next: review and land each item under "Awaiting review" below as its session reports done.
- **Eval (R4)** on `eval/wave1-corrections` (`01eaf61`, pushed, not in `dev`). E1 is done; `dev` is merged in (`e7d3e44`, labels regenerated byte-identical).
  E2: the first 32-row sheet was an AI review, kept as `eval/grading/p3-calib-2026-09-24/warm/scores-ai-review.csv`; `scores.csv` is blank again for the user's own grades.
  Then a recalibration session runs `judge --calibration` (about $0.45, approved) and `--calibrate` against the 0.70 kappa bar; the first attempt failed all 91 calls on a sandbox DNS error and spent nothing ([`EVAL_REBUILD.md`](EVAL_REBUILD.md) "Calibration packet (2c)" and `migration/P3_CALIBRATION_REVIEW_2026-09-24.md`, both on that branch).
  E3, the final two-arm capture judged twice with `--final`, is spend-approved and waits on calibration and the user's go-ahead. R4 spend so far about $7.44 of $20.
- **Answer quality.** Q1 landed on `dev` (current time, reading ages rounded up, the report's status reason, spike and note guidance; `SPECS.md` §10.2). Q3-Q6 (current site, stuck sensors, over-wide limits, CWA Old) are next in one session cut from `dev`; Q8 holds Q1's re-check gaps.
- **Upstream.** Both `feature/gilligan-rag-assistant` branches are pushed (server `b2074b8`, dashboard `da5412f`); PRs are held back ([`migration/UPSTREAM_PR_BODIES.md`](migration/UPSTREAM_PR_BODIES.md)). The user merges and deploys after the demo.
  The malware did run on a build machine; the user was told every credential was rotated. Build only from the clean feature branches ([`migration/SECURITY_INCIDENT_2026-09-19.md`](migration/SECURITY_INCIDENT_2026-09-19.md)).

## Awaiting review

- **Sol, P3/P4 follow-up**: server and dashboard `task/gilligan-release-p3-p4` (`78e2dfb`, `40e59b8`, fetched from `/tmp/cer-p3p4-*`). Requested fixes: the null-organization predecessor rule (`timeline.md` 2026-09-24), revert the `.eslintrc.js` parser change, check chart handling of null turbidity. Sol commits in `/tmp` clones because the upstream `.git` directories are read-only to it; fetch its branches in after review.
- **Sol, stale docs**: `docs/stale-o7` in `/tmp/cer-stale-o7` (`d54793e`), recording O6, O7 and S5 outside STATUS and the plan.
- **Service (S1-S4, Q7)**: `feat/service-release` (`8d61134`, `cc8a300`, pushed) in `.claude/worktrees/feat+service-release`: image packaging with hash-checked corpus and cache, shared-secret service check, Firestore usage store, model-call gate. Server half: `feat/service-key` (`9ef59b7`) in `~/code/clean-earth-rovers/worktrees/server-service-key`, cut from `local`, cherry-picks cleanly onto the feature branch; its suite passed 12/12 with `tsc` clean.
- **L2 interview**: `docs/l2-inputs` in `.claude/worktrees/l2-inputs`, no commits yet.

## Not started

- **Hygiene L3 (Luna)**: never ran. The `deviceApi` test, the `.agents` copy of `git-plan`, the OneDrive paths in `.claude/settings.json`, and the leaked `/tmp/eval-fixtures-*` directories.
- **L1 runbook rewrite** for the user deploying (due Sep 26): [`migration/GILLIGAN_DEPLOYMENT_RUNBOOK.md`](migration/GILLIGAN_DEPLOYMENT_RUNBOOK.md); L2 answers so far are in the plan.
- **Dashboard session** (U1-U6, P5) from the published dashboard branch.

## Last session

- Reviewed Q1, E1, the R4 merge, P1, token cap, P3/P4 and the service key; landed three branches on `dev`; fixed the catalogue prompt test (`1fe7a40`); ages round up (`53620b4`).
- Checks after landing: typecheck and lint clean; `prompt`, `readingAge`, `listPods`, `querySensorData`, `generateReport`, `reportModel` passing singly. Spend: none by this session; Q1's session spent 10 approved paid live turns.
- Deleted stale branches `eval/claims-reresolve`, server `develop`, dashboard `main`; fetched Sol's P3/P4 branches upstream.
- Reset the calibration sheet after finding the first grading was an AI review (`01eaf61`); reopened E2 (`dd4b51a`).

## Working tree

- `dev` level with `origin/dev` after this handoff; `_EXIT_CRITERIA.md`, `eval/grading/phase-1d-wave1-fixture-review.html`, `review-marked-up.html` and the root v2 PDF stay untracked on purpose (`migration/LOCAL_STACK.md`).
- cer-demo worktrees: `wave1-corrections` (R4; untracked `data/results/judge/p3-calib-2026-09-24/`, an empty failed-run summary), `feat+service-release` (locked), `l2-inputs`, `hygiene` (no work). Merged and removable once their sessions close: `answer-quality-q1` (locked), `token-cap`, `upstream-publish`.
- Upstream: server `local` `d12ad6d`, dashboard `local` `fd103a0`, with git-ignored `.env` files; publish worktrees under `~/code/clean-earth-rovers/worktrees/`.
- Git-ignored or untracked restored inputs: `node_modules/`, `.env`, the corpus PDFs, `.ocr_cache/`, `data/corpus/`, `data/embeddings/cache.json`. Still missing: `data/retrieval-eval/`, `data/device-fields/`, `data/backend-surface/`, `serviceAccountKey.json`.

## Open work

- User: grade the reset calibration sheet (E2); answer whether a non-existent organization id counts as null for CWA Old; start the Q3-Q6, L1, dashboard and hygiene sessions; finish the L2 interview; CER's Fireworks key and budget (S5); F2 if not already approved.
- User, later: approve E3's start, T2's live write batches, and each upstream push.
- Agent: review and land the "Awaiting review" items; keep the plan current; on Sep 28 move open "should" tasks after launch and freeze the release candidate (L4).
- Standing: re-seed Firestore needs approval; the slice-coverage overshoot and `ADVICE_TIER` design wait for after launch.

## Unfixed defects

| where | defect | severity |
|---|---|---|
| `Dockerfile` + `.dockerignore` on `dev` | The image excludes `data/`, so it ships without corpus or embedding cache; fixed on `feat/service-release`, not landed. | high |
| `src/quota/InMemoryQuotaStore.ts` on `dev` | Quotas reset on restart and multiply per instance; the Firestore store is on `feat/service-release`, not landed. | medium |
| server `findPeriodWaterData` | Device filters are not checked against membership (`migration/SECURITY_FINDINGS.md` §1); fixed on `task/gilligan-release-p3-p4`, which denies CWA Old to CWA until the null-organization rule lands. | high |
| server `.eslintrc.js` (`78e2dfb`) | With the parser configured, `npm run lint` (`eslint --fix src`) rewrites .ts files and still reports 623 errors. | medium |
| server `findPeriodWaterData` | Slices an organization's labels to 10 for the `in` query, dropping pods past the tenth (pre-existing). | low |
| server `GilliganService.askQuestionGemini` | Calls the retired `gemini-pro`; production Gilligan fails every question and `gemini` is not a rollback (D9). | high |
| `scripts/judge.ts` (wave1) | Exits 0 and writes an empty summary when every call fails. | medium |
| tools-on answers | Withheld-history note dropped, water-type note misparaphrased, implausibly high dissolved oxygen not flagged (plan Q8). | medium |
| `test/unit/deviceApi.test.ts` | Its error-code list lacks `quota_reports_exceeded`, so the suite fails. | low |
| a Jest suite (unidentified) | Leaves `/tmp/eval-fixtures-*` directories behind. | low |
| `../user-dashboard` `src/app/confirm-email` | Imports `confirmEmail`, which `services/auth` does not export; build reports "Attempted import error" (plan P5). | medium |
| `../user-dashboard` `src/app/gilligan/page.js` | `useSearchParams` outside Suspense deopts the page to client rendering. | low |
| citation contract | Citations carry only `source`, so a document shows as its address (plan U5). | low |
| `eval/fixtures-wave1/` | Slice-coverage overshoot: 84/90 turns have explanatory sources outside the ◆G9 slice. | medium |

## Active traps

- **Upstream history carries malware**; branch tips are clean. It runs on `next dev`, `next build` and `npm test`. Never check out `main`, `develop` or an old commit; scan with `grep -rlE ' {200,}' --exclude-dir=node_modules --exclude-dir=.git .` after any clone, fetch, pull or branch switch.
- **`SENSOR_TOOL` and `REPORT_TOOL`**: on means live production reads; `.env` sets both `true`, so captures and judge runs must set both `false` explicitly on server and runner.
- Both servers read `.env` at boot only. `hybrid-slice-vector` needs the git-ignored embedding cache; a fresh worktree has no `node_modules`, `.env` or `data/` (link per the `run-local` skill). `EnterWorktree` cuts from `origin/main`; reset onto `dev`.
- Re-ingesting with a different tesseract build moves 12 chunk ids and voids labels; `resolveRetrievalLabels.ts` does not delete stale label files.
- Anything in `documents/` is ingested; keep the v2 PDF at the repo root.
- `.claude/settings.json` deny rules point at old OneDrive paths, so the upstream read-only guard is inert.
- Run Jest suites singly with `--runInBand`; use port 8010, never kill 8000.
- The device token is superadmin (5 pods) with no `exp` claim.
- Eval captures and judge runs need `--run=<id>`; the judge ledger has no answer hash. A judge exit status of 0 does not prove calls succeeded.
- Sandboxed sessions (Codex) may lack network and write access to upstream `.git`; they work in `/tmp` clones, which a restart can erase.
- Nothing is deployed; all testing is local.

## Where things live

- **Release**: [`migration/GILLIGAN_RELEASE_PLAN.md`](migration/GILLIGAN_RELEASE_PLAN.md), [`migration/GILLIGAN_DEPLOYMENT_RUNBOOK.md`](migration/GILLIGAN_DEPLOYMENT_RUNBOOK.md), [`migration/GILLIGAN_FIRESTORE_FRAMEWORK.md`](migration/GILLIGAN_FIRESTORE_FRAMEWORK.md), [`migration/LIVE_TEST_DATA.md`](migration/LIVE_TEST_DATA.md).
- **Gilligan**: [`migration/GILLIGAN_TARGET_ARCHITECTURE.md`](migration/GILLIGAN_TARGET_ARCHITECTURE.md) (decisions D1-D12), [`migration/GILLIGAN_R3_PORT.md`](migration/GILLIGAN_R3_PORT.md), [`migration/TASK_C_HANDOFF.md`](migration/TASK_C_HANDOFF.md), [`migration/CONVERSATION_QA_2026-09-24.md`](migration/CONVERSATION_QA_2026-09-24.md).
- **Environment and security**: [`migration/LOCAL_STACK.md`](migration/LOCAL_STACK.md), [`migration/SECURITY_INCIDENT_2026-09-19.md`](migration/SECURITY_INCIDENT_2026-09-19.md), [`migration/SECURITY_FINDINGS.md`](migration/SECURITY_FINDINGS.md), [`migration/BACKEND_FIELDS.md`](migration/BACKEND_FIELDS.md).
- **Behaviour and decisions**: [`SPECS.md`](SPECS.md), [`timeline.md`](timeline.md), [`migration/CONVENTIONS.md`](migration/CONVENTIONS.md).
- **Evaluation**: [`EVAL_REBUILD.md`](EVAL_REBUILD.md) (full R4 state on `eval/wave1-corrections`), [`GRADING_GUIDE.md`](GRADING_GUIDE.md).
- **People**: [`STAKEHOLDER_QUESTIONS.md`](STAKEHOLDER_QUESTIONS.md), [`ARCHIVED.md`](ARCHIVED.md), house rules in [`../CLAUDE.md`](../CLAUDE.md) and `.claude/skills/`.
